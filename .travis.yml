language: python
python:
  - '3.7'

before_script:
  - pip install -r requirements-dev.txt
  - cd pur_beurre
  - pwd
  - ls -la

# safelist
branches:
  only:
    - travis4

env: DJANGO_SETTINGS_MODULE="pur_beurre.settings.travis"

services:
  - postgresql

script:
  - pytest -v
