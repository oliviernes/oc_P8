language: python
python:
  - '3.7'

before_script:
  - cd pur_beurre
  - pwd
  - ls -la

# safelist
branches:
  only:
    - travis4

env: DJANGO_SETTINGS_MODULE="pur_beurre.settings"

services:
  - postgresql

script:
  - pytest pur_beurre/
